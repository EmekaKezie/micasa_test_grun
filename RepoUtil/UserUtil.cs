using System.Reflection;
using TheEstate.Data.Database;

namespace TheEstate.RepoUtil
{
    public class UserUtil
    {
   //     public static async Task<bool> CreateUserUtil(UserModelCreate model,string? id = null)
   //     {
   //         try
   //         {
   //             object[] param = new object[] { id, model.Username!, model.Password, DateTime.Now, Constants.ActiviyStatus.PENDING.ToString(), Utility.CreateRandomPassword(4), DateTime.Now };
   //             int action = await db.SQLExecuteAsync(SQL.CreateUserQry, param);
   //         }
   //         catch (Exception)
   //         {

			//	throw;
			//}
   //     }
    }
}
